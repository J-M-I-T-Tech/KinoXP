spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/KinoXP?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Europe/Copenhagen
    username: kino
    password: kino
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

  sql:
    init:
      mode: always